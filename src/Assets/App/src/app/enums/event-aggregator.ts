export enum Channels {
    DragDrop = 'dboard:drag-drop',
    DragDropStatus = 'dboard:drag-drop:status',
}

export enum DragDropAction {
    Enable = 'enable',
    Disable = 'disable',
    Toggle = 'toggle',
}

export enum DragDropStatus {
    Enabled = 'enabled',
    Disabled = 'disabled',
}
